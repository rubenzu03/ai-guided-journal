services:
  web:
    build: .
    command: uvicorn asgi:application --host 0.0.0.0 --port 8000 --workers 1 --proxy-headers
    ports:
      - "8080:8000"
    environment:
      - DEBUG=0
      - SECRET_KEY=changeme
      - ALLOWED_HOSTS=0.0.0.0,localhost
    restart: unless-stopped
